# --- Database ---
# sqlite  = in-memory (Render free tier, sem billing)
# postgres = PostgreSQL local (docker-compose)
DATABASE_URL=sqlite://
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/idempotency

APP_ENV=development
LOG_LEVEL=INFO
ALLOWED_HOSTS=*
BACKEND_CORS_ORIGINS=*
